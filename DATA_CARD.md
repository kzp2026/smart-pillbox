# 智能药盒评论数据卡

输入：`data/京东智能药盒评论.csv`。510条记录，字段为昵称、日期、评分、产品、评论。来源平台仅由文件名声明为京东；采集工具、抽样方法、商品链接/ID、授权、是否含激励好评以及用户代表性尚无可验证资料，状态为unverified_source。不能把仓库文件存在当作真实用户实验或授权证明。

固定清洗：UTF-8 CSV解析，评论NFKC规范化、连续空白合并、首尾空白删除，min_length=1；按规范化后的原文本完全去重。按输入记录顺序分配C0001等ID，同一输入重复运行不变；输入排序或数据变更属于新数据集，ID必须连同run_id和输入SHA-256引用。source_row是包含表头的逻辑记录行号，并非跨行CSV文本的物理行号。

实测：510条，空值0条，过短0条，重复10条，合并10条，有效500条。全部510条保存在cleaning_audit.json/xlsx；重复行的duplicate_of指向保留ID。未为了获得500而随机删行。只保留评分、日期、产品版本、来源渠道；不将昵称列放入分析、映射、生成或评价输出。

审计的original_comment保留原评论文本（手机号和邮箱遮盖）；cleaned_comment保存规范化文本。匿名处理在去重判断之后执行，避免两个不同电话号码遮盖后被错误合并。原始输入副本仅在run/private/input.csv保留，以便核验输入哈希，其中仍包含原始昵称，不是公开产物。公开前应人工审查评论正文中的姓名、地址及其他识别信息；自动遮盖并非完全匿名化保证。

标签/评分现状：仓库没有可用于本次新协议的真实人工需求标注、专家映射审核、A/B/C盲评或V1—V2配对二次评价。程序只生成空模板。`verify_e2e.py`产生的SIM审核和评分是明确模拟的测试fixture，不允许与正式实验合并。历史output映射缺少完整可复现来源，因此只读标注legacy，不能迁入新的approved图谱。
